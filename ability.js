var Ability = function(id, caster){
	var self = {
		type:"",
		id: id,
		caster: caster
	}
	self.dash = function(args){
		self.caster.velX += barges
	}
	self.cast =function() {
		if (type == "directional boost"){
			strength = 10;
			self.dash(caster.direction)

		}
		else if ()
	}
}